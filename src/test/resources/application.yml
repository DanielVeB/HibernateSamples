spring:
  datasource:
    url: jdbc:h2:mem:mydb;TRACE_LEVEL_FILE=4;DB_CLOSE_ON_EXIT=-1
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
